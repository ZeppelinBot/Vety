{
  "name": "Vety devenv",

  "dockerComposeFile": "../docker/devenv/compose.yml",
  "initializeCommand": "docker compose -f ./docker/devenv/compose.yml up -d --wait",

  "service": "devenv",
  "remoteUser": "dev",
  "workspaceFolder": "/workspace/vety"
}
